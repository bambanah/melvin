#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

yarn commitlint --edit "$1"

npx lint-staged

yarn test ||
	(
		printf '\n❌📝 Tests failed 📝❌\n'
		false
	)

yarn build ||
	(
		printf '\n❌👷 Better call Bob 👷❌\n'
		false
	)
