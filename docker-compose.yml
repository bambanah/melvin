version: "3.6"
services:
  mysql-db:
    container_name: mysql-db
    image: mysql:8
    ports:
      - "3309:3306"
    restart: unless-stopped
    volumes:
      - db_data:/var/lib/mysql
    environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD=true
volumes:
  db_data:
