# Implementation Plans

Generated by the improve skill on 2026-07-02, audited at commit `c48e1dd`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

**Selection note**: the operator was away when the audit finished, so per the skill's non-interactive default the top findings by leverage were planned (the recommended "top 6" set). Findings #7–#14 below were vetted but not planned; ask for plans if wanted.

## Execution order & status

| Plan | Title                                                        | Priority | Effort | Depends on | Status |
| ---- | ------------------------------------------------------------ | -------- | ------ | ---------- | ------ |
| 001  | Require auth + ownership on all PDF generation paths         | P1       | S      | —          | TODO   |
| 002  | Move the NextAuth secret to the environment and rotate it    | P1       | S      | —          | TODO   |
| 003  | Enforce `ownerId` scoping across the tRPC routers            | P1       | M      | —          | TODO   |
| 004  | Start the weeknight rate at 8pm, not 7pm                     | P1       | S      | —          | TODO   |
| 005  | Characterization tests for trip transit + activity cost math | P1       | S–M    | 004        | TODO   |
| 006  | One source of truth for the Provider Travel per-km rate      | P1       | M      | 001, 005   | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 005 requires 004 so the tests lock in the corrected 8pm boundary rather than characterizing the bug.
- 006 requires 001 (it edits `generatePDF`, which 001 changes to `(invoiceId, ownerId)`) and 005 (its rate changes need the characterization safety net).
- 001, 002, 003, 004 are mutually independent and can run in any order or in parallel worktrees.

## Vetted findings not yet planned

(Evidence verified by the advisor at `c48e1dd`; ask for a plan if you want one.)

- **#7 Group-activity invoice creation double-writes** — `invoice-router.ts:44-62` creates group entries under the invoice's main client AND `:238-261` creates them again (correct client, but orphaned from the invoice); an inverted `useEffect` guard at `invoice-activity-creation-form.tsx:137-143` wipes `groupClientId` whenever the group selector is shown. NOTE: there is uncommitted WIP on the group-activity form in the working tree — reconcile before planning.
- **#8 Trip mutation robustness** — all five trip mutations do sequential per-activity updates with no `$transaction`; the "already in a trip" guard in `trip.create` can never fire (`tripActivitySelect` at `trip-router.ts:13-34` doesn't select `tripId`, so the check at `:61-64` always sees `undefined`); `activity.bulkAdd` skips both overlap checking and trip transit calculation; removing a middle trip activity silently zeroes the bridging leg.
- **#9 Payment matching collapses identical totals** — `invoice-router.ts:393-408` keys a Map by total amount; `invoice-utils.ts:63-96` treats each amount as usable once, so two $100 invoices can't match a $200 payment.
- **#10 Quick fixes** — `test:unit` runs vitest in watch mode (use `vitest run`); `activity.forInvoice` RETURNS a `TRPCError` as data (`activity-router.ts:180-182`); `if (!record)` after `prisma.delete` is dead code (Prisma throws P2025 instead); `isHoliday` only correct for the current year (`date-utils.ts:46-57`); `formatDuration` wraps at ≥24h (`date-utils.ts:21-38`); dead code: `getEffectiveTransitRate` becomes live after plan 006, `getTotalInvoiceHours` stub returns `69` (`date-utils.ts:40`), orphaned `src/components/forms/date-picker.tsx`.
- **#11 Over-fetching** — `activity.pending` is unbounded with full relations; `defaultActivitySelect` pulls whole `supportItem`/`client` rows; `generatePDF` issues two queries where one would do.
- **#12 Duplicate trip modals** — `trip-builder-modal.tsx` / `trip-edit-modal.tsx` (~850 near-identical lines incl. the 30-min cap logic).
- **#13 Dependency consolidation** — `date-fns` used in 4 files for `format()` only (dayjs is the standard here); `@headlessui/react` used once (`invoice-page.tsx:8`) where `ui/dialog.tsx` exists; the `radix-ui` umbrella package has zero imports.
- **#14 CI gaps** — no production `next build` in CI; Playwright browsers re-downloaded every run (no cache keyed on Playwright version).
- **Router-level integration test harness** — biggest remaining test gap after plan 005; needed to properly regression-test plan 003's ownership rules.

## Findings considered and rejected

- **`pnpm audit` advisories (19)**: all transitive and unreachable — the DOMPurify stack sits under jsPDF's `html()` path, which this codebase never calls (PDFs are built with `text`/`autoTable`); the HIGH `fast-uri` items are under Prisma's dev toolchain, not runtime. Re-check if jsPDF usage changes.
- **"Wire up overlap detection" as a feature**: stale — `checkActivityOverlap` is already enforced on `activity.add` and `activity.modify`; the only gaps are `bulkAdd` and invoice-created activities (folded into unplanned finding #8).
- **next-auth v4 → v5 migration**: lag, not breakage; blast radius is small (3 files) but auth is critical and v4 still receives fixes. Revisit when v4 EOLs or blocks a Next upgrade.
- **Renovate effectively disabled** (`renovate.json` disables all packages): deliberate manual-update posture, visible in the frequent `chore: update deps` commits. Note the deprecated `config:base` preset name if ever re-enabling.
- **`tsconfig.tsbuildinfo` / `.DS_Store` / `test-results/` in tree**: all gitignored; no action.
- **Docker-compose `postgres:pass` credentials**: local-dev defaults, by design.
- **ADR-documented behaviors** (one-way transit semantics, manual inter-client entry, modifiable invoiced trips): settled decisions, not findings.

## Doc staleness noted (not planned)

- `docs/plans/architecture-deepening/` cites PDF rates `0.43`/`0.85`; code at `c48e1dd` uses `0.43`/`0.99`. Plan 006 removes the `0.43` entirely — mark that doc stale when 006 lands.
- `CONTEXT.md` mentions `groupTransitRatePerKm`, removed from the schema in `3d7c5a9`.
